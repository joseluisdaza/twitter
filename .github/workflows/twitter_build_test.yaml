name: twitter_build_test_legacy

on:
  workflow_dispatch:

jobs:
  moved-to-new-workflows:
    name: Workflow legado (solo informativo)
    runs-on: ubuntu-latest
    steps:
      - name: Aviso de migracion
        run: |
          echo "Este workflow fue reemplazado por:"
          echo "- ci_validate.yaml (CI: build/test/validaciones)"
          echo "- cd_deploy.yaml (CD: despliegue)"
          echo "Ejecuta los nuevos workflows desde la pestaña Actions."

